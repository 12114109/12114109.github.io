window.addEventListener("load",init);

function init()
{
    function ChooseForm()
    {
        var selectedOption = document.getElementById("type_register").value;
        console.log(selectedOption);
    }

}

